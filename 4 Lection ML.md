Connected with [[3 Seminar ML]] and continue [[3 Lection ML]]
### Обучение линейных моделей
(начало было на семинарах)
![[MSE в таблицах.png]]
Далее считаем градиент и считаем веса линейной модели:
$$
\begin{aligned}
\nabla Q(w) = 0
\\w_{*} = (X^TX)^{-1}X^Ty
\end{aligned}
$$
**Проблемы**:
1) $x^Tx$ - может быть необратимой
2) обращение $x^Tx : O(d^3)$( где матрица $x^Tx$ размера $d\cdot d$)
3) если $L(y,z)$ хитрая, то $\nabla Q(w) = 0$ может не иметь аналитического решения

---
### Градиентные методы обучения
$$
\begin{aligned}
Q(w_{1},\dots,w_{d}) \to \min_{w}
\\Пусть\ Q - дифференцируема
\\на\ всякий\ \nabla Q(w) = \left( \frac{dQ}{dw_{1}} \dots \frac{dQ}{dw_{d}}\right)
\end{aligned}
$$
**Важные свойства градиента:**
1) $\nabla Q(w)$ показывает в сторону наискорейшего роста, a $-\nabla Q(w)$ - в сторону наискорейшего убывания
2) $\nabla Q(w)$ ортогонален минимум уровня
![[градиентный спуск.png]]
Градиентный спуск (gradient descent - GD)
$$
\begin{aligned}
w^0 - инициализация(случайная)
\\шаг:
\\w^k = w^{k-1} - \eta\nabla Q(w^{k-1}), где\ \eta - длина\ шага\ (learning\ rate\ или\ гиперпараметр, \eta > 0)
\end{aligned}
$$
останавливаемся, если:
- $|\nabla Q(w^k)| < \epsilon$
- $|w^k-w^{k-1}| < \epsilon$
- $|Q(w^k) - Q(w^{k-1})| < \epsilon$
- k > N (то есть номер шага больше какого то лимита)
- если ошибка на валидационной выборке перестала убывать
Замечания:
1) Локальные минимумы
	![[мультистарт-1.png]]
	решение $\to$ мультистарт
2) много условий сходимости
	- функция Q(w) - выпуклая и дифференцируемая
	- $\nabla Q(w)$ - липшицева, т.e. $|\nabla Q(w_{1}) - \nabla Q(w_{2})| \leq L|w_{1}-w_{2}|$ 
	- $\eta$ - не очень большая ($\leq \frac{1}{L}$)
	Если все 3 пункта выполнены $\to$ Градиентный спуск (GD) сойдется и будет глобальный минимум
3) с линейными моделями и адекватными функциями, тогда Q(w) будет выпукла почти всегда 
4) $Q(w^k)- Q(w_{*}) = O\left( \frac{1}{k} \right)$, где k - номер итерации, а $w_{*}$ - точка оптимума
#### Оценивание градиента
$$
\begin{aligned}
Q(w) = \frac{1}{\ell}\sum_{i = 1}^{\ell}L(y_{i}, a(x_{i};\ w)), где\ L(y_{i}, a(x_{i};\ w) = q_{i}(w)
\\Q(w)= \frac{1}{\ell}\sum_{i = 1}^{\ell}q_{i}(w)
\\\nabla Q(w) = \frac{1}{\ell}\sum_{i = 1}^{\ell}\nabla q_{i}(w)
\\Это\ плохо\ так\ как\ если\ даже\ \ell = 10^6 \to надо\ 10^6\ градиентов \to
\\GD\ медленный
\end{aligned}
$$
##### Стохастический градиентный спуск (SGD)
$$
\begin{aligned}
\\\nabla Q(w) \sim \nabla q_{i}(w)
\\тогда\ шаг\ SGD:
\\i_{k} - случайный\ индекс\ объекта\ (i_{k} \sim U(1,\dots,\ell))
\\w^k=w^{k-1} - \eta\nabla q_{i_{k}}(w^{k-1})
\end{aligned}
$$
объяснение:
![[SGD-1.png]]
как решить проблему, что около минимума, SGD везет себя плохо:
идея - затормаживать градиентный спуск в этот момент
$$\begin{aligned}
w^k = w^{k-1} - \eta_{k}\nabla q_{i_{k}}(w^{k-1})
\\ надо (условие\ Робинса-Монро):
\\ \sum_{k=1}^{\infty}\eta_{k} = \infty (то\ есть\ ряд\ расходится)
\\ \sum_{k=1}^{\infty}\eta_{k}^2 < \infty (то\ есть\ ряд\ сходится)
\\ если\ выполнено\ \to SGD - сойдется\ к\ минимуму
\\(при\ выпуклости,\ липшиц.,\ и\ если\ угадаем\ с\ w^0)
\end{aligned}
$$
Но есть проблема:
$\mathbb{E}\left( Q_{w^k} - Q(w_{*}) = O\left( \frac{1}{\sqrt{ k }} \right) \right)$
Замечания:
1) mini - batch GD, где $\nabla Q(w) \sim \frac{1}{t}\sum_{j=1}^{t}\nabla q_{i}(w)$ 
2) SGD хорош для онлайн - обучения![[онлайн обучение.png]]
##### SAG (stochastic average gradient)
$$
\begin{aligned}
Q(w) = \frac{1}{\ell}\sum_{i = 1} ^ {\ell} q_{i}(w)
\\z_{i}^0 = \nabla q_{i}(w^0)
\\итерация\ SAG:
\\ i_{k} \sim U(1,\dots,\ell)
\\ z_{i}^k = \begin{cases} \nabla q_{i}(w^{k-1}),\ i = i_{k} \\ z_{i}^{k-1},\ i \neq i_{k} \\  \end{cases}
\\ \nabla Q(w^{k-1}) \sim \frac{1}{\ell} \sum_{i = 1}^{\ell}z_{i}^k
\\w^k = w^{k-1} - \eta_{k}\frac{1}{\ell}\sum_{i = 1}^{\ell}z_{i}^k
\\\mathbb{E}\left( Q_{w^k} - Q(w_{*}) = O\left( \frac{1}{ k } \right) \right)
\end{aligned}
$$
