Продолжение [[1 Lection ML]]
### Этапы решения задачи в ML:
1. Постановка задачи
2. Сбор данных и разработка признаков
3. Подготовка данных
4. Выбор loss function
5. Выбор семейства моделей
6. Обучение модели
7. Валидация модели (оффлайн - на исторических данных, онлайн - a/б тесты)
8. Внедрение и эксплуатация модели
9. Эффективность модели

---
## Линейные модели
### Линейные модели регрессии
#### 1. Как они устроены 
$\mathbb{Y} = \mathbb{R}$ 
$\mathbb{X} = \mathbb{R ^ d}$ - каждый объект описывается вещественный d-мерным вектором (все признаки вещественные)
$\mathbb{a}(x) = \mathbb{w_{0}} + \mathbb{w_{1}}\mathbb{x_{1}} + \dots + \mathbb{w_{d}}\mathbb{x_{d}}$ - линейная модель регрессии, 
где $\mathbb{x_{i}}$ - признаки, 
$\mathbb{w_{i}}$ - параметры (конкретно в линейных моделях - веса - weights),
$\mathbb{w_{0}}$ - свободный коэффициент (biass / intercept) 

![[первой преобразование дефолтной формулы.png]]
Предположим - есть константный признак, пусть $\mathbb{x_{1}} = 1$ 
Тогда в нашей формуле получается $\dots + \mathbb{w_{1}\mathbb{x_{1}}} + \dots \to \mathbb{w_{1}}$ - выполняет роль свободного коэффициента 
Теперь формула:
$\mathbb{a}(x) = \mathbb{w}\cdot\mathbb{x}$ - скалярное произведение w на х 

---
#### 2. Применение линейных моделей
$\mathbb{a}(x) = \mathbb{w_{0}} + \mathbb{w_{1}}* площадь\ кв. + \mathbb{w_{2}}* этаж + \mathbb{w_{3}}* расстояние\ до\ метро$  
- признаки влияют на прогноз независимо
- признаки влияют на прогноз линейно
$\to$  **Для линейных моделей данные надо готовить !!!** 
###### 1. Категориальные признаки 
 пусть $\mathbb{x_{j}}$ - категориальный, т.е. $\mathbb{x_{j}} \in {\mathbb{c_{1}},\dots,\mathbb{c_{m}}}$ , к примеру район Москвы
 ###### **one - hot encoding (OHE) - для каждого района заведем свой признак нахождения в этом районе**  
 если квартира, то 1, 0 во всех остальных районах
![[one - hot encoding.png]]
###### one - cold encoding (OCE) - наоборот, 0 там, где квартира, 1 во всех остальных районах

 P.S. - индикаторы
 ![[Индикаторы.png]]
 
 $\mathbb{a}(x) = \mathbb{w_{0}} + \mathbb{w_{1}}*[хамовники?] + \mathbb{w_{2}}* [бассманная?] + \dots$  - зависимость теперь от района
  ###### **2. Дискретизация признаков** 
  ![[дискретизация.png]] где крестики - какие - то объекты
  Видно, что зависимость не линейная, поэтому работать хорошо не будет с линейной моделью
  Можем разбить по порогам:
  ![[костыль.png]]
  Тогда наша формула будет иметь вид ![[new formula.png]]
  и теперь наш график будет более "константный"
  ![[график.png]]
  

---
#### 3. Полиноминальные признаки
Представим есть площадь - $\mathbb{x_{1}}$, этаж - $\mathbb{x_{2}}$, расстояние до метро - $\mathbb{x_{3}}$
Есть проблема, что мы не учитываем взаимодействие этих признаков 
Поэтому добавим новые признаки (т е сделаем все мономы над этими признаками) до 2-ой степени
 $\mathbb{x_{1}^2}$  $\mathbb{x_{2}^2}$  $\mathbb{x_{3}^2}$  $\mathbb{x_{1}}\mathbb{x_{2}}$  $\mathbb{x_{2}}\mathbb{x_{3}}$  $\mathbb{x_{1}}\mathbb{x_{3}}$ 
 Можно повышать степени еще дальше (всякие там корни, логарифмы и т.д.)
 **ПРОБЛЕМА**: 1) не понятно, что из этого брать 2) переобучение
 (т.е. мы добавляем какое то взаимодействие признаков)
 

---
### Измерение ошибки в задаче регрессии
$\mathbb{L}(\mathbb{y}, \mathbb{z})$ - надо придумать
Q( a, X ) = $1/\ell \sum_{i=1}^{\ell} \mathbb{L}(\mathbb{y_{i}, \mathbb{a}(\mathbb{x_{i}})})$ 
MSE - mean squared error (среднеквадратичная ошибка) - $\mathbb{L}(\mathbb{y}, \mathbb{z})  = (y - z)^2 \to Q( a, X ) = 1/\ell \sum_{i=1}^{\ell} (\mathbb{a}(\mathbb{x_{i}}) - \mathbb{y_{i}})^2$ 
ПРОБЛЕМЫ:
1) ![[1) проблема MSE.png]] т.е. получилась другая единица измерения - ответ модели в рублях, правильный ответ в рублях, все в квадрате поэтому получились квадратные рубли -> тут надо считать RMSE (Root MSE или просто взять корень из MSE)
2) по значению RMSE нельзя без контекста понять хорошая ли модель. Как вариант - считать модифицированную версию RMSE или коэффициент детерминации 
   $R^2 = 1 - \sum_{i=1}^{\ell} (\mathbb{a}(\mathbb{x_{i}}) - \mathbb{y_{i}})^2 / \sum_{i=1}^{\ell} (\mathbb{y_{i}} - \mathbb{y^-})^2$, 
   где $\mathbb{y^-} = 1/\ell \sum_{i=1}^{\ell} \mathbb{y_{i}}$  (средний ответ по выборке)
   Почему это имеет смысл?
   пусть модель идеальная, т.е. $\mathbb{a}(\mathbb{x_{i}}) = \mathbb{y_{i}} \to R^2 = 1$ 
   если модель так себе (возьмем средний ответ по выборке) $\mathbb{a}(\mathbb{x_{i}}) = \mathbb{y^{-}} \to R^2 = 0$ 
   для нормальных моделей $0 < R^2  \leq 1$ 
MAE - mean absolute error
$\mathbb{L}(\mathbb{y}, \mathbb{z}) = |y - z| \to Q( a, X ) = 1/\ell \sum_{i=1}^{\ell} |\mathbb{a}(\mathbb{x_{i}}) - \mathbb{y_{i}}|$ 
это про выбросы
1 ситуация
![[1 варинат с выбросом.png]]
2 ситуация 
![[2 ситуация.png]]
т.е. мы подогнали под выброс и сделали ответ поближе, MSE уменьшился -> улучшился, а МАЕ наоборот, ухудшился
MSE стимулирует подгонку под выбросы, а MAE скорее игнорирует выбросы
![[график-1.png]]
Почему не использовать MAE всегда?
- может быть важно учитывать объекты с большими ошибками
- По величине производной в MSE можно понять близость к экстремуму, но у MAE производная всегда +- 1, поэтому сложно в оптимизации ![[производная.png]]
- у MAE вторая производная нулевая, а есть метод обучения со второй производной -> опять проблемы с оптимизацией
---
### Huber Loss
![[Huber Loss.png]]
попытка склеить MAE и MSE
![[график-2.png]]
где d - гиперпараметр, т.e. определяет, что мы считаем за выбросы
отклонения <d приоритетно снижать
Проблема:
- опять со 2 производной
Чтобы это починить есть **LogCosh** 
![[Log cosh.png]]
график будет выглядеть практически так же, но не будет проблем со 2 производной